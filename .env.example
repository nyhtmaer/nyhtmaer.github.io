# Portfolio Configuration
# This is an example .env file. Copy this to .env.local and fill in your details.

# Optional: GitHub API Token (not required for static portfolio)
# Get your token from: https://github.com/settings/tokens
# Only needed if you want to fetch repos dynamically instead of using static config
VITE_GITHUB_TOKEN=your_github_token_here
